
<h1>Posts</h1>
	<div id="posts">
	<% @posts.each do |post|%>
		<div class="post">
			<h3><%=link_to post.title, post %></h3>
			<small>Published at <%= time_ago_in_words(post.created_at) %> by <%= post.user.email %></small>
		</div>
	<% end %>
</div>